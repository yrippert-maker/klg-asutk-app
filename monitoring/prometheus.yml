global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: klg-backend
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ['klg-backend:8000']
